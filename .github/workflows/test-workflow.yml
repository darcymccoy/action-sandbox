name: Test Workflow

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-action:
    name: GitHub Actions Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v5

      - name: Test Action
        id: test-action
        uses: darcymccoy/merge-conflict-action@7c11050a440abdbd46e933b99ffffabf20b95d3d
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          post-comments: true